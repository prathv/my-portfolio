  version: 0.2

  phases:
    install:
      runtime-versions:
       nodejs: 10
    build:
      commands:
       - echo Nothing to do yet
  artifacts:
    files:
      - index.html
      - favicon.ico
      - styles/main.css
      - images/*


  sam deploy \
  --template-file package.yml \
  --stack-name portfolioLambdaDeploy \
  --capabilities CAPABILITY_IAM